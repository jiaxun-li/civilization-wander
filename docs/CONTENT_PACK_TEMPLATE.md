# Civilization Wander V5 内容包空白模板

使用前先阅读 `AGENTS.md` 与 `docs/CONTENT_PACK_AND_AUTHORING_WORKFLOW.md`。严格按三阶段推进；第一、第二阶段结束后停止并等待用户批准。第二阶段内部依次完成证据整理、故事写作和公共文字删减，再把完整正文与内部证据附录合并提交、等待一次批准；第二阶段获批并进入第三阶段后，默认连续完成媒体、置入、验证与复盘，仅在媒体缺口、AI／示意图需要明确批准或结构无法表达时暂停相关部分。

每轮将“内容产物”和“流程改进建议”分开。流程建议只使用以下标签：通用规则、当前选题特例、实现问题、尚待验证。

---

## 第一阶段：选题简报

### 内容产物

**暂定主题：**

**内容包规划入口 Entity：** 仅用于选题、协作与审稿；不自动成为每张 Card 的 `primaryEntityId`

**Entity 类型：** 从当前数据已经使用或用户已经明确批准的类型中选择；当前已批准的宗教与神话类型为 `religionAndMyth`；未经允许不得新增其他类型

**拟包含的 Card 及各自主 Entity：** 每张 Card 均必填一个真正对应其故事主体的 Entity；不得直接复制内容包规划入口

**公共类型标签核对：** 每张 Card 主 Entity 的类型是否已经拥有稳定的公共中文标签

**Entity 首页摘要：** 若类型为 `政治实体` 或 `文化传统`，`canonicalSummary` 必须以与 `timeSpan.label` 完全一致的规范化年代和中文逗号开头，例如“约公元前1600—前1000年，……”

**工作定义：**

**核心叙事假设：**

**用户好奇入口：**

**主叙事时段：**

**必要前史：**

**必要后续：**

**空间范围：**

**目标读者：** 默认不熟悉历史，但对历史感兴趣；若本选题另有更具体的读者条件，在此补充，不得提高默认历史知识门槛

**拟连接的现有 Card：**

**已有来源或图片：**

**数据文件决策：**

- 目标文件：
- 新建或复用：
- 选择理由：
- 聚合方式与加载顺序（新建文件时）：

**若使用“第一次”入口，其内部分类：**

- [ ] 目前已知最早
- [ ] 现存材料中最早的一批或之一
- [ ] 早期关键转折点

**主要证据缺口或选题风险：** 包括会实质影响选题成立或造成证据越界的边界问题；不另设“明确不写”固定栏目

### 流程改进建议

- 通用规则：
- 当前选题特例：
- 实现问题：
- 尚待验证：

**阶段状态：** 等待用户批准。

---

## 第二阶段：来源、Claim ledger 与 Card／Scene 完整待审正文

### 内容产物

> 本阶段内部依次完成证据整理、故事写作和公共文字删减，但只在三遍全部完成后提交一次。公共正文与内部证据附录是不同产物；正文只能使用 Claim ledger 中去向为 `public` 的 Claim，不得逐条翻译来源限制、证据边界或 EditorialReview。

### 公共故事骨架

| Scene | 读者看见的人／物 | 发生的行动、处境或变化 | 必须出现的专名及其叙事作用 | Public Claim ID |
|---:|---|---|---|---|
| 1 |  |  |  |  |
| 2 |  |  |  |  |
| 3 |  |  |  |  |

> 专名不设跨 Card、跨内容包或整张 Card 的数量上限。一个名称能否出现，只看它是否推动人物关系、事件进展、历史判断或跨故事连接，不看相关 Card 或扩展包是否已经制作。删去后不影响这些作用的名称，应改成日常指称或移入内部材料。多个必要名称不得压缩成清单，应按叙事顺序逐个引入。

### Card 公共文字

**暂定标题：**

**引言（通常一至两句话，只打开故事入口）：**

**核心叙事：**

**时间范围：**

### Scene 1／通常共 3–11 个

**标题：**

**时间范围／界面年代小字：** 根据数值 `start`／`end` 填写纯年份 `timeSpan.label`，例如“约公元前2350—前2150年”或“约公元前700—公元400年”；不得填写王朝、世纪、考古阶段、文本传统、地点、主题或材料

**时间显示模式：** `year`／`undatedNarrative`；默认 `year`。只有没有可考历史发生年份的史诗或神话内部情节使用 `undatedNarrative`，固定显示“叙事时间 · 无可考年份”，不得自定义文案

**当前读者问题：**

**叙事任务：**

**具体行动、处境或变化：**

**允许进入公共正文的 Claim ID：**

**Event ID 与 `kind`：** 至少一项；`historicalEvent`／`historicalProcess`／`textualTradition`／`traditionalNarrative`

**时间相交核对：** 至少一个所引 Event 的规范化时间与本 Scene 相交；可与其他 Scene 复用同一 Event，不为本 Scene 虚构一次性 Event

**完整正文：**

**来源 ID：**

> 按相同结构复制到本 Card 实际需要的 Scene 总数；通常为 3–11 个，但不是数量门禁。数量由该 Card 的重要性与完整叙事需要决定；不得为了凑数量机械拆分，也不得为了压缩数量遗漏必要情节或解释步骤。每个 Scene 必须至少有一项来源。

### 公共正文删减审查

**正文中保留的专名及其叙事作用：**

**已删除的非必要专名：**

**已改成日常指称的名称：**

**现代学者姓名、学派或争论过程：** 正常情况下应为空；若当前 Card 本身讲述史学史、思想史、考古研究史或某位学者，说明保留理由

**已移入 EditorialReview 的限制、反例、争议或替代解释：**

**公共正文保留的必要限定及原因：** 只有删除后会把推测写成事实、扩大时间空间或因果范围，或强化会误导主线的流行说法时才填写

**连续专名检查：** 是否仍有同一句或同一小段连续引入多个名称，却没有具体行动或变化承接；若有，先调整正文再提交

> 公共正文使用证据支持的最窄正面表述。限制、反例、争议和替代解释默认进入 EditorialReview；只有省略后会实质改变故事含义的限定，才用一句日常语言紧贴相关事实保留。Scene 通常以行动、处境变化或新的历史问题结束，不以研究警告代替叙事收束。

### 内部证据附录

#### 来源清单

| 临时 ID | 来源 | 作者/机构 | 年份 | 类型 | 支持范围 | 内部使用边界 |
|---|---|---|---:|---|---|---|
| source-01 |  |  |  |  |  |  |

> “内部使用边界”用于来源审查和证据控制，不得直接改写成公共正文中的防御性段落。来源作者或现代学者姓名可以保留在此，但不能因此自动进入故事正文。

#### Claim ledger

| Claim ID | 证据允许采用的最窄正面表述 | 类型 | 来源 ID | 证据强度与适用范围 | 去向 | 关联 Card／Scene |
|---|---|---|---|---|---|---|
| claim-01 |  | 事实／学术解释／编辑综合／不确定性／叙事过渡 |  |  | public／editorialReview／dataOnly／delete |  |

> 公共正文只能使用去向为 `public` 的 Claim。不确定性、争议、反例和替代解释默认进入 `editorialReview`；内部数据关系进入 `dataOnly`；来源不足且没有内部审稿价值的内容进入 `delete`。

#### Scene 来源覆盖预检

| Scene 序号 | 暂定任务 | 至少一项来源 | 缺口 |
|---:|---|---|---|
| 1 |  |  |  |
| 2 |  |  |  |
| 3 |  |  |  |

#### Card 内部编辑与数据映射

**Card 主 Entity（必填，对应 `primaryEntityId`）：**

**与内容包规划入口的关系：** 相同／不同；若不同，说明各自承担的作用

**内部编辑目的：**

**由各 Scene 推导的相关 Event：** Card 不手写 `eventIds`；按 Scene 顺序列出并去重

**相关 Entity／StructuralEdge：**

**建议导航及其叙事价值：**

**导航互链检查：** 每个目标 Card 是否只有一个入口；既有 Card 是否需要补充反向入口；文案是否统一使用“进入”而非“返回／回到”；是否默认从目标故事开头进入，任何直达指定 Scene 的 `entry: { kind: 'targetScene' }` 是否已经获得明确批准。

> Event、Entity 和 StructuralEdge 是数据映射，不是公共正文点名清单。相关对象尚未拥有独立 Card，不妨碍它在当前故事中出现。当前只需提及时可以不建立导航；关系或未来导航需要稳定身份时，可以先建立最小但准确的 Entity 或 Event，后续内容继续复用同一 ID。

#### 内部 EditorialReview

- limitations：
- counterexamples：
- uncertainties／disputes：
- alternative explanations：
- source IDs：

> EditorialReview 必须完整，但不得通过把其中内容写入公共正文来满足这一内部要求。若公共正文确需保留一句限定，仍应在这里记录其完整依据、适用范围和替代解释。

### 流程改进建议

- 通用规则：
- 当前选题特例：
- 实现问题：
- 尚待验证：

**阶段状态：** 等待用户批准来源、Claim ledger 与正文；本阶段不选择或审核媒体，不修改项目文件或运行时数据。

---

## 第三阶段：媒体、置入、验证与复盘

### 内容产物

> 只为第二阶段已经批准的正文选择媒体，不在本阶段改写正文。必须先实际检索；新选历史素材或复用既有图片都至少比较两个合适候选并记录理由，不得把“优先复用”当作跳过检索。第三阶段默认连续执行，只有媒体缺口、AI／示意图需批准或结构无法忠实表达时暂停。图片须在最终验收中列明实际画面、来源与许可，并按原始比例完整展示。同一张物理图片在整个当前 V5 中最多用于两个 Scene；不同 Asset ID、title、alt、裁切或转码不能绕过上限。

### Scene 1／与第二阶段 Scene 一一对应

**Scene 标题：**

**媒体类型：** 图片／地图／其他；纯文字须请求用户明确批准

**媒体的理解目的：**

**素材方式：** 复用现有本地素材／寻找现成历史素材／用户明确批准的示意图

**媒体决策类型：** `newHistoricalAsset`／`approvedReuse`／`map`／`aiGenerated`／`textOnly`

**实际查看的候选（新素材或复用至少两项）：** 逐项填写 URL、机构、许可、画面是否对应本 Scene，以及选择／不选择理由

**选择或复用理由：** 不得只写“已有”或“方便”；说明它为何最能帮助理解当前 Scene

**实际图片：** 使用图片时在此提交实际画面，不得只写网址或文字描述

**来源与许可：**

**创作者／机构与年代：**

**历史对象对应关系：**

**原始宽高：**

**图片展示方式：** 按原始比例完整展示，不裁切、不放大填充、不以局部截取代替完整画面

**WebP 交付：** 文件名 _____；编码后 _____ × _____ px；_____ bytes；必须为 `.webp`、严格小于 `500,000 bytes`，且任一边不超过 `2560` px

**编码后视觉检查：** desktop／mobile；细节、文字、细线、透明背景与色彩是否保持；不得改变已批准裁切、宽高比、构图或叙事内容

**拟用 title：**

**拟用 alt：**

**必要 caption：**

**AI 图片标记（使用 AI 生成图时）：** 已明确标注并提交实际画面审核；同样遵守上述 WebP、尺寸和体积规则

**地图图形编码（使用地图时）：** 逐项列出点／线／箭头／色块／纹理及其含义

**地图文字标注（使用地图时）：** 列出必须直接标名的地点，以及紧邻地图解释各图形含义的简短图例；不得让读者猜测点、线或色块代表什么

**地图近似性与来源（使用地图时）：**

**媒体缺口或风险：**

> 按相同结构复制到全部 Scene。进入第三阶段的授权允许选择并置入合规媒体，但不允许改写已批准正文。

### 模型与实现可行性预检

- 当前 schema 能否忠实表达：
- validator 是否支持：
- renderer 是否支持：
- 是否存在实现问题：

### 并行集成交接记录

> 单 Agent 连续完成本内容包时也应简要记录实际所有者；多 Agent 并行时必须完整填写。模块级通过不等于已经接入主运行时。“集成中”只写在本记录或任务状态中，不得写成未经 schema 批准的 runtime 字段。

**是否为多 Agent 并行任务：** 是／否

**Content Agent：**

**Content Agent 所有权范围：** `data/<内容模块>.ts`、`assets/images/<内容模块>/`、模块专属测试和本 ContentPack 记录

**Integration Agent：** 同一时间只能有一个；负责 `index.html`、`src/main.ts`、`src/data/atlas-data.ts`、固定加载顺序测试、跨模块与反向导航及共享验证清单

**UI Agent（如有）：**

**UI 稳定 fixture：** 常规 UI 测试使用的独立有效 fixture；不得依赖正在置入或暂时无效的全集数据

**模块文件：**

**预期加载位置：**

**TypeScript 命名导出：**

**对应 Asset 目录：**

**新增顶层 ID：**

**复用的外部 ID：**

**拟议出站导航：**

**既有模块所需反向导航及建议位置：** 只由 Integration Agent 实际修改

**机器可读交接文件：** `docs/content-packs/<module>.handoff.json`；使用 `handoffVersion: 3`，以 `docs/content-packs/module-handoff.example.json` 为结构起点；新增 ID、活动 atlas 复用 ID、模块文件、TypeScript 命名导出与冻结状态必须和实际文件一致

**pending sibling 外部引用：** 在全部十四个顶层集合下逐项列出；当前活动 atlas 已有引用由门禁自动发现，不手工重复

**模块门禁命令：** `node scripts/validate-content-module.js data/<module>.ts docs/content-packs/<module>.handoff.json`

**模块级语法检查：**

**模块命名导出／精确接口检查：**

**模块内部重复 ID 检查：**

**Source、Asset provenance 与本地文件检查：**

**Asset manifest：** `assets/images/<module>/manifest.json`；使用 `manifestVersion: 2`，新 Asset 的 creator、license、sourceUrl、origin、原始宽高、编码后宽高、字节数、`format: webp`、SHA-256 与 `reviewStatus: approved` 已齐全

**模块内部引用检查：**

**模块专属负向测试：**

**未解决的集成问题：**

**Content Agent 已停止编辑并冻结交接：** 是／否；未确认时 Integration Agent 不得开始接入

**共享文件独占所有权确认：** 当前没有其他 Agent 同时修改入口、聚合器、共享导航或固定集成清单

**非运行时集成状态：** 模块编写中／模块级通过／集成中／已接入；不得添加 `integrationStatus` runtime 字段

> 模块处于“集成中”时，不得部分接入 `src/data/atlas-data.ts`，也不得绕过聚合器由 `src/main.ts` 直接加载。不得通过 renderer 过滤、查询静默跳过、try/catch 压制或删除无效对象来隐藏未完成接入。

### 流程改进建议

- 通用规则：
- 当前选题特例：
- 实现问题：
- 尚待验证：

**媒体状态：** 默认连续置入；仅媒体缺口、AI／示意图待批或结构无法表达时暂停，并在此记录具体例外。

### 置入 V5

### 批准范围

**用户批准原文或等同确认：**

**获批准内容：**

**未获批准内容：**

### 置入记录

**实际数据文件：**

**新建／复用：**

**聚合与加载顺序：**

**新增 ID：**


**复用 ID：**

**本地 Assets：**

**内容数据 JS 对应素材目录：** `assets/images/<数据文件名>/`

**素材目录归属校验：**

**全局图片复用校验：** 同一原始图像或同一 `src` 最多用于两个 Scene；不得用不同 Asset ID、title、alt、裁切或转码绕过

**实现冲突与处理：**

#### 置入阶段流程改进建议

- 通用规则：
- 当前选题特例：
- 实现问题：
- 尚待验证：

### 验证、流程复盘与新增 content 方向

### 内容与实现验收

**直接 Card／Scene 地址：**

**validator：**

**TypeScript 类型与 JavaScript 支持脚本语法检查：**

**运行时清单一致性检查：**

**全量测试：**

**正式 Vite 构建：**

**生产浏览器门禁：**

**desktop：**

**mobile：**

**direct hash／back-forward／滚动恢复：**

**reduced motion：**

**浏览器控制台：**

**未完成或无法实机验证：**

### 流程复盘

- 通用规则：
- 当前选题特例：
- 实现问题：
- 尚待验证：

### 新增 content 方向

| 暂定主题／标题 | 当前故事线索 | 为什么值得继续点开 | 可能的主 Entity／Event／连接目标 | 已有来源基础 | 尚需研究 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

**最终状态：** 等待用户阅读验收。
